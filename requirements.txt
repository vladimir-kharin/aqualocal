# ============================================================================
#  AquaLocal — зависимости. Windows + NVIDIA GPU (CUDA).
#  Установка:  .venv\Scripts\python.exe -m pip install -r requirements.txt
# ============================================================================

# --- Движок распознавания речи (STT) ---
faster-whisper==1.1.1
ctranslate2==4.5.0
tokenizers==0.21.0

# --- Звук и матрицы ---
sounddevice==0.5.1
numpy==2.1.3

# --- Интеграция с ОС (глобальные хоткеи, буфер обмена) ---
keyboard==0.13.5
pyperclip==1.9.0

# --- Иконка в системном трее ---
pystray
Pillow

# --- Сборка (некоторые пакеты требуют setuptools < 82) ---
setuptools<82

# --- CUDA / cuDNN для CTranslate2 (не требуют установки CUDA в систему) ---
# Тянут ~2 ГБ. Нужны для работы Whisper на GPU. Версии не пинуем: pip подберёт
# совместимые с ctranslate2 4.5.0. Если словишь конфликт — пинани руками.
nvidia-cublas-cu12
nvidia-cudnn-cu12

# --- Нечёткое сопоставление имён проектов/тегов после Whisper ---
# Без него код падает на difflib из stdlib (качество ниже, но работает).
rapidfuzz
